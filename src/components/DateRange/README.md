```jsx inside Markdown
<DateRange
  onChange={()=> {}}
  moveRangeOnFirstSelection={false}
  ranges={[
    {
      startDate: new Date(),
      endDate: null,
      key: 'selection'
    }
  ]}
  className={'PreviewArea'}
/>
```

```jsx inside Markdown
<DateRange
  editableDateInputs={true}
  moveRangeOnFirstSelection={false}
  ranges={[
    {
      startDate: new Date(),
      endDate: null,
      key: 'selection'
    }
  ]}
  className={'PreviewArea'}
/>
```
